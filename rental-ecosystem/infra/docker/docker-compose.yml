version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: rental-ecosystem-db
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: rental_ecosystem
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - rental-network

  redis:
    image: redis:7-alpine
    container_name: rental-ecosystem-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    command: redis-server --appendonly yes --loglevel warning
    volumes:
      - redis_data:/data
    networks:
      - rental-network

volumes:
  postgres_data:
    driver: local
  redis_data:
    driver: local

networks:
  rental-network:
    driver: bridge